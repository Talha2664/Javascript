// try...catch is used to handle errors in JavaScript.

// try → contains code that may throw errors.
try{
    console.log(x);
}
// catch → handles the errors.
catch(err){
    console.log(err.message);
    
}
// finally → runs always, even if no error occurs.
finally{
    console.log("Finally always runs!");
    
}

