// 4. for...of Loop                 // used in string and array
// we use it when we only care about values, not indexes.

student = ["talha","yasir","rana"]
for (let val of student) {
    console.log(val);
}